services:
  next-app:
    container_name: transcendence-app
    build:
      context: ./srcs
      dockerfile: Dockerfile
    volumes:
      - ./srcs:/app
      - /app/node_modules
    restart: no
    ports:
      - 3000:3000
